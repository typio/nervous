import { Tensor } from "../tensor";
/** create tensor with number m OR each value of a tensor m subtracted from each value of input tensor  */
export declare const minus: (a: Tensor, s: number | Tensor, axis?: number) => never;
