import randomNormalWGSL from './randomNormal.wgsl?raw'

import { Tensor } from "../tensor";

export const randomNormal = (shape: number[], mean?: number, std?: number) => {
    throw new Error('Method is not yet implemented in webgpu backend 😞')
}