{
  "name": "@light/demos",
  "type": "module",
  "version": "0.0.1",
  "description": "Light demos",
  "scripts": {
    "dev": "concurrently \"esbuild src/main.ts --bundle --sourcemap --outfile=src/build/out.js --watch\" \"http-server src/ -s -p 3333\"",
    "build": "esbuild src/main.ts --bundle --minify --sourcemap --outfile=src/build/out.js"
  },
  "keywords": [],
  "author": "",
  "devDependencies": {
    "concurrently": "^7.4.0",
    "http-server": "^14.1.1"
  },
  "dependencies": {
    "esbuild": "^0.15.10",
    "p5": "^1.4.2"
  }
}